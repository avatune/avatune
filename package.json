{
  "name": "@avatune/root",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "biome format --write",
    "check-types": "turbo run check-types",
    "storybook": "turbo run storybook",
    "storybook:composed": "turbo run storybook --filter=@avatune/example --filter=@avatune/vue-example --filter=@avatune/svelte-example --filter=@avatune/vanilla-example -- --no-open & sleep 1 && bun run storybook --filter=@avatune/storybook-entry"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "turbo": "^2.5.8",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
